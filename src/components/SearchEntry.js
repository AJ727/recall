import React from 'react';

// Component Specification: This component will search through all entries in firebase,
// and return a list of all entries containing the query

export default class SearchEntry extends React.Component {
    
}